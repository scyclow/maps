{
  "name": "maps",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "minify": "node replaceGlobals.js && uglifyjs premin.js --config-file uglify.config.json -o min.js"
  },
  "dependencies": {
    "uglify-js": "^3.15.4"
  }
}
